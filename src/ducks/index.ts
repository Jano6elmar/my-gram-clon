export {default as Users} from './Users' //exporta lo que en el archivo user se está exporando por defecto pero colocale el nombre de users
export {default as Posts} from './Posts' //exporta lo que en el archivo user se está exporando por defecto pero colocale el nombre de users
